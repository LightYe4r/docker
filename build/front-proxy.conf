events {
    worker_connections 1024;
    }
http {
    upstream front-lb {
        server frontend:80;
    }

    server {
        listen 80 default_server;
        
        location / {
            proxy_pass http://front-lb;
        }
    }
}

